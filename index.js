import mongoose from "mongoose";
import { server } from "./app.js";
import {
  IP_SERVER,
  PORT,
  DB_USER,
  DB_PASSWORD,
  DB_HOST,
  DB_AUTH,
  DB_NAME,
  DB_PORT,
} from "./constants.js";
import {} from "./socket.js";
